import PlaceholderPanel from "@/components/shared/PlaceholderPanel";

export default function AdminSettingsPage() {
  return <PlaceholderPanel title="Settings" description="Configure platform-wide settings, notifications, and integrations." />;
}
